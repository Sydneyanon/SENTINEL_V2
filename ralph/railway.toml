# Railway configuration for Ralph autonomous loop
[build]
builder = "DOCKERFILE"
dockerfilePath = "ralph/Dockerfile"

[deploy]
startCommand = "bash /app/ralph/ralph.sh --tool claude 10"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
