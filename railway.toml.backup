# Railway configuration - FORCE Dockerfile usage for Ralph
[build]
builder = "DOCKERFILE"
dockerfilePath = "ralph/Dockerfile"

[deploy]
startCommand = "bash /app/ralph/ralph.sh --tool claude 30"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
